import { primary, gray } from "./color";
import { HREF, PAGE } from "./link";
export * as constant from "./constant";
export * as badge from "./badge";

export const content = {
  primary,
  gray,
};

export const NAV = {
  HREF,
  PAGE,
};
